import { escapeXML } from '../../modules/render/index.mjs';

export function comparisonPage(results, model) {
  const labels = new Map(model.entities.map((entity) => [entity.id, entity.label]));
  const percent = (value) => `${Math.round(value * 100)}%`;
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Pipeline reading experiment · Waxwing</title>
<style>
:root{color-scheme:light;font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#203343;background:#f5f7fa}*{box-sizing:border-box}body{margin:0}main{max-width:1320px;margin:auto;padding:36px 32px 64px}header small{font-size:12px;letter-spacing:.12em;color:#54747a;font-weight:750}h1{font-size:34px;letter-spacing:-1px;margin:14px 0 12px}p{line-height:1.6;margin:10px 0}.lede{max-width:860px;color:#526473}a{color:#216b72;text-underline-offset:3px}.sequence{display:flex;flex-wrap:wrap;gap:9px;align-items:center;margin:23px 0 26px}.sequence span{background:white;border:1px solid #d9e2e8;border-radius:8px;padding:9px 12px;font-size:13px}.sequence i{font-style:normal;color:#8b9aa4}.summary{display:grid;grid-template-columns:1fr 1fr;gap:16px}.score{border:1px solid #d7e1e6;border-radius:12px;background:white;padding:19px 22px}.score strong{display:block;font-size:26px;margin:8px 0}.score p{font-size:13px;color:#526473;margin-bottom:0}.tag{font-size:11px;font-weight:750;letter-spacing:.09em}.score:first-child{border-top:3px solid #bd8140}.score:last-child{border-top:3px solid #3d8581}.toolbar{margin-top:28px;display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap}button{font:inherit;cursor:pointer;background:white;border:1px solid #cad7df;border-radius:7px;padding:9px 13px;font-size:13px;color:#314b5b}button[aria-pressed=true]{color:white;background:#254955;border-color:#254955}.buttons{display:flex;gap:6px}.panel{border:1px solid #d7e1e6;border-radius:12px;background:white;margin-top:12px;overflow:hidden}.caption{padding:15px 20px;border-bottom:1px solid #e3e9ed;font-size:14px;line-height:1.5}.viewport{padding:12px;overflow:auto;min-height:255px;max-height:730px;background:#fff}.viewport img{display:block;margin:0 auto}.viewport.fit img{max-width:100%;height:auto}.legend{font-size:13px;padding:12px 20px;background:#fafcfd;border-top:1px solid #e3e9ed;color:#526473}.links{margin:12px 0;font-size:13px;display:flex;gap:20px}.explanation{display:grid;grid-template-columns:1fr 1fr;gap:32px;margin-top:28px}h2{font-size:19px;margin:0 0 12px}table{border-collapse:collapse;width:100%;font-size:13px}th,td{text-align:left;border-bottom:1px solid #dae3e9;padding:10px 7px}th{color:#637785;font-weight:600}.explanation p{font-size:14px}footer{border-top:1px solid #d7e1e6;margin-top:28px;padding-top:18px;font-size:12px;color:#677d89}@media(max-width:700px){main{padding:24px 16px}h1{font-size:27px}.summary,.explanation{grid-template-columns:1fr}.sequence{gap:6px}.sequence span{font-size:11px;padding:6px}.viewport{max-height:550px}}
</style></head><body><main>
<header><small>WAXWING / REPRODUCIBLE LAYOUT EXPERIMENT 01</small><h1>Does dependency layout explain a processing sequence?</h1>
<p class="lede">One fictional system. Two deterministic arrangements. The reading task is to follow one successfully processed image from upload to its index record.</p></header>
<div class="sequence" aria-label="Established processing sequence">${results.sequence.map((ref, i) => `${i ? '<i>→</i>' : ''}<span>${i + 1}. ${escapeXML(labels.get(ref))}</span>`).join('')}</div>
<div class="summary"><article class="score"><span class="tag">CURRENT PRODUCTION LAYOUT</span><strong>${results.baseline.advancesRight} / 5 transitions advance right</strong><p>${results.baseline.movesLeft} moves back to the left · ${results.baseline.canvas.width}px wide · fits 1200px at ${percent(results.baseline.fitScaleAt1200px)}</p></article>
<article class="score"><span class="tag">EXPERIMENTAL PROCESSING PLACEMENT</span><strong>${results.comparison.advancesRight} / 5 transitions advance right</strong><p>${results.comparison.movesLeft} moves back to the left · ${results.comparison.canvas.width}px wide · fits 1200px at ${percent(results.comparison.fitScaleAt1200px)}</p></article></div>
<div class="toolbar"><div class="buttons"><button id="current" aria-pressed="true">Current layout</button><button id="processing" aria-pressed="false">Processing layout</button></div><div class="buttons"><button id="fit" aria-pressed="true">Fit width</button><button id="actual" aria-pressed="false">100% · scroll</button></div></div>
<section class="panel"><div id="caption" class="caption" aria-live="polite">Current: services are placed in one column; queues and datastore in another. Following the image requires two returns to the left column.</div>
<div id="viewport" class="viewport fit" tabindex="0" aria-label="Scrollable diagram"><img id="diagram" src="current.svg" alt="Actual output of the current Waxwing layout for the image processing fixture"></div>
<div class="legend">Arrows remain actor → resource in both drawings. A consumer still points to its queue. The processing sequence above is separately established by the fixture.</div></section>
<div class="links"><a id="viewer" href="current.html">Open full inspector ↗</a><a id="json2" href="current.json">View this JSON 2</a><a href="model.json">Same JSON 1</a><a href="results.json">Measured results</a></div>
<section class="explanation"><article><h2>Criterion chosen before the run</h2><p>Each next stage must lie entirely to the right of the previous stage. This is a specific reading objective, not a universal score for diagram quality. No human comprehension study was performed.</p>
<table><thead><tr><th>Processing transition</th><th>Current</th><th>Experiment</th></tr></thead><tbody>${results.baseline.transitions.map((step, i) => `<tr><td>${escapeXML(labels.get(step.from))} → ${escapeXML(labels.get(step.to))}</td><td>${step.advancesRight ? 'Right' : step.movesLeft ? 'Left' : 'Same column'}</td><td>${results.comparison.transitions[i].advancesRight ? 'Right' : 'Not right'}</td></tr>`).join('')}</tbody></table></article>
<article><h2>What the comparison actually shows</h2><p>The current placement follows operation direction. Processing moves in the opposite direction for the two consumption relationships.</p><p>The experiment reverses those two relationships only in ELK’s temporary placement graph, then restores their original arrow direction. It uses no LLM and no hand-tuned coordinates. The complete original model survives both exports and both layouts pass the existing validator.</p><p><strong>The tradeoff:</strong> the processing arrangement is about ${(results.comparison.canvas.width / results.baseline.canvas.width).toFixed(1)} times wider. At fit width its labels are much smaller. It meets the sequence criterion; it does not win every reading task.</p><p>Changing only the model’s question to ask about dependencies ${results.checks.questionOnlyChangeLeavesGeometryIdentical ? 'leaves the current geometry identical' : 'changes the current geometry'}. That is a control observation, not a claim that every question needs a different drawing.</p></article></section>
<footer>Experiment only. Production layout and JSON contracts are unchanged. The consumer placement rule is justified for this fixture’s stipulated sequence; it is not a general inference about causality.</footer>
</main><script>
const captions={current:'Current: services are placed in one column; queues and datastore in another. Following the image requires two returns to the left column.',processing:'Experiment: all six stages advance right, but the canvas is much wider. At Fit width labels shrink; choose 100% and scroll to inspect. Consumer arrowheads still point toward their queues.'};
for(const name of ['current','processing'])document.getElementById(name).addEventListener('click',()=>{for(const other of ['current','processing'])document.getElementById(other).setAttribute('aria-pressed',String(name===other));const img=document.getElementById('diagram');img.src=name+'.svg';img.alt=name==='current'?'Actual current layout output':'Experimental processing placement with original operation arrows';document.getElementById('caption').textContent=captions[name];document.getElementById('viewer').href=name+'.html';document.getElementById('json2').href=name+'.json';document.getElementById('viewport').scrollTo(0,0);});
for(const name of ['fit','actual'])document.getElementById(name).addEventListener('click',()=>{document.getElementById('viewport').classList.toggle('fit',name==='fit');for(const other of ['fit','actual'])document.getElementById(other).setAttribute('aria-pressed',String(name===other));});
</script></body></html>`;
}
